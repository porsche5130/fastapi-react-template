# PA6.4 系統部署指南

## 一、專案結構說明

```
W:\P-PA6.4\
├── Develop\              ✅ 生產環境需要
│   ├── backend\          ✅ 後端應用程式（需部署）
│   │   ├── app\          ✅ 應用程式核心
│   │   ├── migrations\   ✅ 資料庫遷移 SQL
│   │   ├── backups\      ❌ 備份檔案（不需部署）
│   │   └── requirements.txt
│   └── frontend\         ✅ 前端應用程式（需部署）
│       ├── src\
│       ├── public\
│       └── package.json
│
├── DevTools\             ❌ 開發工具（不需部署）
│   ├── check_*.py        ❌ 檢查工具
│   ├── backup_*.py       ❌ 備份工具
│   └── run_*.py          ❌ 遷移執行器
│
└── 系統設計\              ❌ 設計文件（不需部署）
    └── 應用系統設計\
```

---

## 二、部署檢查清單

### 部署前檢查
- [ ] 確認目標環境資料庫已建立
- [ ] 確認環境變數設定正確（DATABASE_URL, REDIS_URL 等）
- [ ] 確認網路連線正常
- [ ] 備份目標環境現有資料（如有）

### 需要部署的目錄
- [ ] `Develop/backend/` - 後端應用程式
- [ ] `Develop/frontend/` - 前端應用程式

### 不需要部署的目錄
- [ ] `DevTools/` - 開發工具
- [ ] `系統設計/` - 設計文件
- [ ] `Develop/backend/backups/` - 資料庫備份
- [ ] `.git/` - Git 版本控制（如有）
- [ ] `node_modules/` - 前端依賴（會重新安裝）
- [ ] `__pycache__/` - Python 快取

---

## 三、部署步驟

### 方法一：手動複製（推薦新手）

#### Windows 環境

**步驟 1: 複製後端**
```cmd
xcopy /E /I /EXCLUDE:排除清單.txt "W:\P-PA6.4\Develop\backend" "D:\Production\backend"
```

**步驟 2: 複製前端**
```cmd
xcopy /E /I /EXCLUDE:排除清單.txt "W:\P-PA6.4\Develop\frontend" "D:\Production\frontend"
```

**排除清單.txt 內容：**
```
backups\
__pycache__\
*.pyc
.pytest_cache\
node_modules\
.env.local
```

#### Linux/macOS 環境

**步驟 1: 複製後端**
```bash
rsync -av --exclude='backups/' --exclude='__pycache__/' --exclude='*.pyc' \
  W:/P-PA6.4/Develop/backend/ /var/www/production/backend/
```

**步驟 2: 複製前端**
```bash
rsync -av --exclude='node_modules/' --exclude='.env.local' \
  W:/P-PA6.4/Develop/frontend/ /var/www/production/frontend/
```

---

### 方法二：Git 部署（推薦進階）

**步驟 1: 設定 .gitignore**
```gitignore
# 開發工具（不提交到 Git）
/DevTools/

# 設計文件（不提交到 Git）
/系統設計/

# 備份檔案
/Develop/backend/backups/

# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
.pytest_cache/

# Node.js
node_modules/
npm-debug.log*

# 環境變數
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
```

**步驟 2: 提交到 Git**
```bash
git add Develop/
git commit -m "Release version X.X.X"
git push origin main
```

**步驟 3: 在生產環境拉取**
```bash
cd /var/www/production
git pull origin main
```

---

### 方法三：Docker 部署（推薦專業）

**Dockerfile (後端)**
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 只複製需要的檔案
COPY Develop/backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY Develop/backend/app ./app
COPY Develop/backend/migrations ./migrations

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**Dockerfile (前端)**
```dockerfile
FROM node:18-alpine AS builder

WORKDIR /app

# 只複製需要的檔案
COPY Develop/frontend/package*.json ./
RUN npm ci

COPY Develop/frontend/ ./
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
```

**docker-compose.yml**
```yaml
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
```

---

## 四、部署後檢查

### 後端檢查
```bash
# 檢查服務運行
curl http://localhost:8000/health

# 檢查資料庫連線
curl http://localhost:8000/api/health/db

# 檢查 Redis 連線
curl http://localhost:8000/api/health/redis
```

### 前端檢查
```bash
# 檢查前端可訪問
curl http://localhost:80

# 檢查 API 連線
# 登入系統測試
```

### 功能檢查清單
- [ ] 登入功能正常
- [ ] 組織資料可查詢
- [ ] 使用者管理功能正常
- [ ] 權限控制正常
- [ ] 系統通知功能正常
- [ ] 檔案上傳功能正常

---

## 五、回滾計畫

### 如遇到問題，立即回滾

**方法一：使用備份還原**
```bash
# 停止服務
systemctl stop backend frontend

# 還原備份
cp -r /backup/backend_YYYYMMDD/ /var/www/production/backend/
cp -r /backup/frontend_YYYYMMDD/ /var/www/production/frontend/

# 重啟服務
systemctl start backend frontend
```

**方法二：Git 回滾**
```bash
git log  # 找到上一個穩定版本的 commit ID
git reset --hard <commit-id>
git push --force origin main  # 謹慎使用
```

---

## 六、常見問題

### Q1: 不小心複製了 DevTools 怎麼辦？
**A**: 在生產環境刪除即可，不影響系統運行：
```bash
rm -rf /var/www/production/DevTools
```

### Q2: 如何確認沒有複製開發工具？
**A**: 檢查生產環境目錄結構：
```bash
ls /var/www/production/  # 應該只有 backend/ 和 frontend/
ls /var/www/production/backend/  # 應該沒有 check_*.py 等檔案
```

### Q3: migrations 目錄要部署嗎？
**A**: 要！`Develop/backend/migrations/` 包含資料庫建表 SQL，是必要的。

### Q4: backups 目錄要部署嗎？
**A**: 不要！`Develop/backend/backups/` 是開發環境的備份，不應部署到生產環境。

---

## 七、安全建議

1. **環境變數分離**: 生產環境使用獨立的 `.env` 檔案
2. **密碼強度**: 生產環境資料庫密碼必須足夠複雜
3. **HTTPS**: 生產環境必須啟用 HTTPS
4. **防火牆**: 只開放必要的埠（80, 443）
5. **定期備份**: 每日自動備份資料庫
6. **日誌監控**: 啟用錯誤日誌監控

---

## 八、聯絡資訊

如有部署問題，請聯絡：
- 開發團隊負責人：[待填寫]
- 技術支援：[待填寫]

---

**最後更新**: 2026-01-26
**文件版本**: 1.0.0
**維護者**: 開發團隊
