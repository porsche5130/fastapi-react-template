# 系統管理後台資料表設計

## 專案資訊
- **專案名稱**: Paris Agreement Article 6.4 管理系統
- **專案代碼**: PA6.4
- **文件版本**: 1.0.0
- **最後更新**: 2026-01-18

---

## 資料表架構總覽

### 系統管理後台資料表

| 資料表名稱 | 英文名稱 | 用途 | 類型 |
|-----------|---------|------|------|
| 作業紀錄表 | `userlogs` | 記錄所有使用者操作行為 | 交易紀錄 |
| 系統設定檔 | `sys_profile` | 系統全域設定（唯一一筆） | 系統設定 |
| 組織單位明細檔 | `organizations` | 多租戶組織管理 | 主檔 |
| 系統功能明細檔 | `sysfuction` | 系統功能選單與權限 | 主檔 |
| 使用者明細檔 | `user_detail` | 使用者帳號資料 | 主檔 |
| 使用者角色明細檔 | `user_role` | 角色群組定義 | 主檔 |

---

## 1. 作業紀錄表 (userlogs)

### 用途
記錄所有使用者在系統中的操作行為，包含 CRUD、列印、檔案操作等。

### 資料表結構

```sql
CREATE TABLE userlogs (
    -- 主鍵
    id SERIAL PRIMARY KEY,

    -- 作業資訊
    user_detail_id INTEGER NOT NULL,
    sysfuction_id INTEGER NOT NULL,
    module_item VARCHAR(50) NOT NULL CHECK (module_item IN ('Create', 'Read', 'Update', 'Delete', 'Print', 'File')),

    -- 資料記錄
    look_data JSONB NOT NULL DEFAULT '{}',
    change_data JSONB NOT NULL DEFAULT '{}',

    -- 時間與錯誤
    action_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    err_detail VARCHAR(2000),

    -- 索引
    INDEX idx_userlogs_user (user_detail_id),
    INDEX idx_userlogs_function (sysfuction_id),
    INDEX idx_userlogs_action_at (action_at),
    INDEX idx_userlogs_module (module_item),

    -- 外鍵
    FOREIGN KEY (user_detail_id) REFERENCES user_detail(id),
    FOREIGN KEY (sysfuction_id) REFERENCES sysfuction(id)
);

-- 註解
COMMENT ON TABLE userlogs IS '作業紀錄表';
COMMENT ON COLUMN userlogs.id IS '資料編號';
COMMENT ON COLUMN userlogs.user_detail_id IS '作業人員';
COMMENT ON COLUMN userlogs.sysfuction_id IS '作業功能';
COMMENT ON COLUMN userlogs.module_item IS '模組項目 (Create/Read/Update/Delete/Print/File)';
COMMENT ON COLUMN userlogs.look_data IS '檢視資料 (JSON 格式: {欄位名稱: 資料內容})';
COMMENT ON COLUMN userlogs.change_data IS '異動資料 (JSON 格式: {欄位名稱: 資料內容})';
COMMENT ON COLUMN userlogs.action_at IS '作業時間';
COMMENT ON COLUMN userlogs.err_detail IS '異常紀錄';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, AUTO | 自動編號 |
| 作業人員 | user_detail_id | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 作業功能 | sysfuction_id | INTEGER | 8 | NOT NULL | 外鍵: sysfuction.id |
| 模組項目 | module_item | VARCHAR | 50 | NOT NULL | Create/Read/Update/Delete/Print/File |
| 檢視資料 | look_data | JSONB | - | NOT NULL | 欄位名稱: 資料內容 |
| 異動資料 | change_data | JSONB | - | NOT NULL | 欄位名稱: 資料內容 |
| 作業時間 | action_at | TIMESTAMP | - | NOT NULL | 預設當前時間 |
| 異常紀錄 | err_detail | VARCHAR | 2000 | NULL | 錯誤訊息 |

### JSON 格式範例

```json
// look_data 範例
{
  "organization_id": 1,
  "org_name": "匠耘有限公司",
  "contact_email": "porsche@lab.taipei"
}

// change_data 範例 (Update 操作)
{
  "before": {
    "contact_email": "old@example.com"
  },
  "after": {
    "contact_email": "new@example.com"
  }
}
```

---

## 2. 系統設定檔 (sys_profile)

### 用途
系統全域設定，控制系統維護狀態與基本資訊。**此表只有一筆資料 (id=1)**。

### 資料表結構

```sql
CREATE TABLE sys_profile (
    -- 主鍵 (固定為 1)
    id INTEGER PRIMARY KEY DEFAULT 1 CHECK (id = 1),

    -- 系統狀態
    is_service BOOLEAN NOT NULL DEFAULT TRUE,

    -- 系統資訊
    sys_url VARCHAR(200) NOT NULL,
    sys_ctitle VARCHAR(200) NOT NULL,
    sys_etitle VARCHAR(200) NOT NULL,
    sys_ccopyright VARCHAR(200) NOT NULL,
    sys_ecopyright VARCHAR(200) NOT NULL,

    -- 管理資訊
    sys_organization INTEGER NOT NULL DEFAULT 1,
    sys_mana_email VARCHAR(200) NOT NULL,

    -- 系統欄位
    edit_by INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,

    -- 外鍵
    FOREIGN KEY (sys_organization) REFERENCES organizations(id),
    FOREIGN KEY (edit_by) REFERENCES user_detail(id)
);

-- 註解
COMMENT ON TABLE sys_profile IS '系統設定檔 (唯一一筆)';
COMMENT ON COLUMN sys_profile.id IS '資料編號 (固定為 1)';
COMMENT ON COLUMN sys_profile.is_service IS '系統狀態 (1:正常, 0:維護)';
COMMENT ON COLUMN sys_profile.sys_url IS '系統網址';
COMMENT ON COLUMN sys_profile.sys_ctitle IS '系統中文標題';
COMMENT ON COLUMN sys_profile.sys_etitle IS '系統英文標題';
COMMENT ON COLUMN sys_profile.sys_ccopyright IS '系統中文版權宣告';
COMMENT ON COLUMN sys_profile.sys_ecopyright IS '系統英文版權宣告';
COMMENT ON COLUMN sys_profile.sys_organization IS '系統管理公司';
COMMENT ON COLUMN sys_profile.sys_mana_email IS '系統管理員電子郵件';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, 固定=1 | 只有一筆 |
| 系統狀態 | is_service | BOOLEAN | 1 | NOT NULL, DEFAULT=1 | 1:正常, 0:維護 |
| 系統網址 | sys_url | VARCHAR | 200 | NOT NULL | 系統網址 |
| 系統中文標題 | sys_ctitle | VARCHAR | 200 | NOT NULL | 中文標題 |
| 系統英文標題 | sys_etitle | VARCHAR | 200 | NOT NULL | 英文標題 |
| 系統中文版權 | sys_ccopyright | VARCHAR | 200 | NOT NULL | 中文版權 |
| 系統英文版權 | sys_ecopyright | VARCHAR | 200 | NOT NULL | 英文版權 |
| 系統管理公司 | sys_organization | INTEGER | 8 | NOT NULL, DEFAULT=1 | 外鍵: organizations.id |
| 管理員郵件 | sys_mana_email | VARCHAR | 200 | NOT NULL | 管理員信箱 |
| 編輯者 | edit_by | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 建立時間 | created_at | TIMESTAMP | - | NOT NULL | 建立時間 |
| 修改時間 | updated_at | TIMESTAMP | - | NULL | 最後修改時間 |

### 系統維護頁面邏輯

| 條件 | 顯示頁面 |
|------|----------|
| 無法取得 API 服務 | 系統維護中頁面 |
| API 回應 `is_service = 1` | 登入頁面 |
| API 回應 `is_service = 0` | 系統維護中頁面 |

---

## 3. 組織單位明細檔 (organizations)

### 用途
多租戶組織管理，支援政府機關、公司行號、個人。

### 資料表結構

```sql
CREATE TABLE organizations (
    -- 主鍵
    id SERIAL PRIMARY KEY,

    -- 組織資訊
    org_code VARCHAR(200) UNIQUE NOT NULL,
    org_name VARCHAR(200) NOT NULL,
    org_type INTEGER NOT NULL CHECK (org_type IN (1, 2, 3)),

    -- 聯絡資訊
    contact_person VARCHAR(200) NOT NULL,
    contact_email VARCHAR(200) NOT NULL,
    contact_phone VARCHAR(200) NOT NULL,
    address VARCHAR(200),
    phone VARCHAR(200),

    -- 系統欄位
    is_mana BOOLEAN NOT NULL DEFAULT FALSE,
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    memo VARCHAR(1000),
    edit_by INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,

    -- 索引
    INDEX idx_organizations_code (org_code),
    INDEX idx_organizations_active (is_active),
    INDEX idx_organizations_mana (is_mana),

    -- 外鍵
    FOREIGN KEY (edit_by) REFERENCES user_detail(id)
);

-- 註解
COMMENT ON TABLE organizations IS '組織單位明細檔';
COMMENT ON COLUMN organizations.org_type IS '組織型態 (1:政府機關, 2:公司行號, 3:個人)';
COMMENT ON COLUMN organizations.is_mana IS '系統管理公司 (只有一家可設為 TRUE)';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, AUTO | 自動編號 |
| 組織統編/代碼 | org_code | VARCHAR | 200 | UNIQUE | 唯一值 |
| 組織名稱 | org_name | VARCHAR | 200 | NOT NULL | 組織名稱 |
| 組織型態 | org_type | INTEGER | 8 | NOT NULL | 1:政府, 2:公司, 3:個人 |
| 連絡人 | contact_person | VARCHAR | 200 | NOT NULL | 連絡人姓名 |
| 連絡人郵件 | contact_email | VARCHAR | 200 | NOT NULL | 連絡人 Email |
| 連絡人電話 | contact_phone | VARCHAR | 200 | NOT NULL | 連絡人電話 |
| 組織地址 | address | VARCHAR | 200 | NULL | 地址 |
| 組織代表號 | phone | VARCHAR | 200 | NULL | 代表號 |
| 系統管理公司 | is_mana | BOOLEAN | 1 | DEFAULT=0 | 只有一家為 TRUE |
| 啟用 | is_active | BOOLEAN | 1 | DEFAULT=1 | 啟用狀態 |
| 備註 | memo | VARCHAR | 1000 | NULL | 備註 |
| 編輯者 | edit_by | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 建立時間 | created_at | TIMESTAMP | - | NOT NULL | 建立時間 |
| 修改時間 | updated_at | TIMESTAMP | - | NULL | 修改時間 |

### 預設資料

```sql
INSERT INTO organizations (
    id, org_code, org_name, org_type,
    contact_person, contact_email, contact_phone,
    address, phone, is_mana, is_active, memo, edit_by, created_at, updated_at
) VALUES (
    1, '82871784', '匠耘有限公司', 2,
    '陳琦', 'porsche@lab.taipei', '0910326333',
    '', '', TRUE, TRUE, '', 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
);
```

---

## 4. 系統功能明細檔 (sysfuction)

### 用途
系統功能選單樹狀結構與權限模組定義。

### 資料表結構

```sql
CREATE TABLE sysfuction (
    -- 主鍵
    id SERIAL PRIMARY KEY,

    -- 功能資訊
    func_code VARCHAR(200) NOT NULL,
    upper_func_id INTEGER NOT NULL DEFAULT 0,
    func_cname VARCHAR(200) NOT NULL,
    func_ename VARCHAR(200) NOT NULL,
    func_type INTEGER NOT NULL CHECK (func_type IN (1, 2)),
    func_order INTEGER NOT NULL,
    func_icon VARCHAR(200),

    -- 模組資訊
    func_module_name VARCHAR(200),
    module_item JSONB NOT NULL DEFAULT '[]',

    -- 說明與狀態
    description TEXT,
    is_mana BOOLEAN NOT NULL DEFAULT FALSE,
    is_active BOOLEAN NOT NULL DEFAULT TRUE,

    -- 系統欄位
    edit_by INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,

    -- 索引
    INDEX idx_sysfuction_code (func_code),
    INDEX idx_sysfuction_upper (upper_func_id),
    INDEX idx_sysfuction_type (func_type),
    INDEX idx_sysfuction_active (is_active),
    INDEX idx_sysfuction_order (func_order),

    -- 外鍵
    FOREIGN KEY (upper_func_id) REFERENCES sysfuction(id),
    FOREIGN KEY (edit_by) REFERENCES user_detail(id),

    -- 約束
    CONSTRAINT chk_func_module CHECK (
        (func_type = 1 AND func_module_name IS NULL) OR
        (func_type = 2 AND func_module_name IS NOT NULL)
    )
);

-- 註解
COMMENT ON TABLE sysfuction IS '系統功能明細檔';
COMMENT ON COLUMN sysfuction.func_type IS '功能類型 (1:節點, 2:功能)';
COMMENT ON COLUMN sysfuction.upper_func_id IS '上層功能 (0為根節點)';
COMMENT ON COLUMN sysfuction.module_item IS '模組項目 JSON 陣列 ["Create","Read","Update","Delete","Print","File"]';
COMMENT ON COLUMN sysfuction.description IS '說明 (富文本格式)';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, AUTO | 自動編號 |
| 功能代碼 | func_code | VARCHAR | 200 | NOT NULL | 功能代碼 |
| 上層功能 | upper_func_id | INTEGER | 8 | NOT NULL | 0=根節點 |
| 中文名稱 | func_cname | VARCHAR | 200 | NOT NULL | 中文名稱 |
| 英文名稱 | func_ename | VARCHAR | 200 | NOT NULL | 英文名稱 |
| 功能類型 | func_type | INTEGER | 8 | NOT NULL | 1:節點, 2:功能 |
| 功能次序 | func_order | INTEGER | 8 | NOT NULL | 排序 |
| 功能圖示 | func_icon | VARCHAR | 200 | NULL | 圖示 |
| 模組名稱 | func_module_name | VARCHAR | 200 | NULL | type=2 必填 |
| 模組項目 | module_item | JSONB | - | NOT NULL | CRUD+Print+File |
| 說明 | description | TEXT | - | NULL | 富文本 |
| 系統管理 | is_mana | BOOLEAN | 1 | DEFAULT=0 | 是否為系統管理功能 |
| 啟用 | is_active | BOOLEAN | 1 | DEFAULT=1 | 啟用狀態 |
| 編輯者 | edit_by | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 建立時間 | created_at | TIMESTAMP | - | NOT NULL | 建立時間 |
| 修改時間 | updated_at | TIMESTAMP | - | NULL | 修改時間 |

### 預設資料

```sql
INSERT INTO sysfuction (
    id, func_code, upper_func_id, func_cname, func_ename, func_type, func_order,
    func_icon, func_module_name, module_item, description, is_mana, is_active, edit_by, created_at, updated_at
) VALUES
(1, 'system_mana', 0, '系統管理後台', 'System Management', 1, 10,
 'settings', NULL, '[]', '', TRUE, TRUE, 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP),

(2, 'sys_profile', 1, '系統設定資料', 'System Profile', 2, 1010,
 'settings', 'sys_profile', '["Create","Read","Update","Delete","Print","File"]', '', TRUE, TRUE, 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP),

(3, 'organizations', 1, '組織設定', 'Organizations', 2, 1020,
 'business', 'organizations', '["Create","Read","Update","Delete","Print","File"]', '', TRUE, TRUE, 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP),

(4, 'user_role', 1, '使用者角色設定作業', 'User Roles', 2, 1030,
 'users', 'user_role', '["Create","Read","Update","Delete","Print","File"]', '', TRUE, TRUE, 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP),

(5, 'user_detail', 1, '使用者設定作業', 'User Details', 2, 1040,
 'user', 'user_detail', '["Create","Read","Update","Delete","Print","File"]', '', TRUE, TRUE, 1, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);
```

---

## 5. 使用者明細檔 (user_detail)

### 用途
使用者帳號與基本資料管理。

### 資料表結構

```sql
CREATE TABLE user_detail (
    -- 主鍵
    id SERIAL PRIMARY KEY,

    -- 組織與帳號
    organization_id INTEGER NOT NULL,
    account VARCHAR(100) UNIQUE NOT NULL,
    username VARCHAR(200) NOT NULL,
    password VARCHAR(200) NOT NULL,

    -- 職務資訊
    department VARCHAR(200),
    job_title VARCHAR(200),
    phone VARCHAR(200),

    -- 角色權限
    user_role JSONB NOT NULL DEFAULT '[]',

    -- 登入資訊
    last_login_at TIMESTAMP,
    last_login_ip VARCHAR(100),

    -- 系統欄位
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    edit_by INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,

    -- 索引
    INDEX idx_user_detail_account (account),
    INDEX idx_user_detail_org (organization_id),
    INDEX idx_user_detail_active (is_active),

    -- 外鍵
    FOREIGN KEY (organization_id) REFERENCES organizations(id),
    FOREIGN KEY (edit_by) REFERENCES user_detail(id)
);

-- 註解
COMMENT ON TABLE user_detail IS '使用者明細檔';
COMMENT ON COLUMN user_detail.user_role IS '所屬角色 JSON 陣列 [user_role.id]';
COMMENT ON COLUMN user_detail.password IS '密碼 (bcrypt hash)';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, AUTO | 自動編號 |
| 組織編號 | organization_id | INTEGER | 8 | NOT NULL | 外鍵: organizations.id |
| 登入帳號 | account | VARCHAR | 100 | UNIQUE | 帳號 |
| 使用者名稱 | username | VARCHAR | 200 | NOT NULL | 姓名 |
| 使用者密碼 | password | VARCHAR | 200 | NOT NULL | bcrypt hash |
| 所屬部門 | department | VARCHAR | 200 | NULL | 部門 |
| 職稱 | job_title | VARCHAR | 200 | NULL | 職稱 |
| 連絡電話 | phone | VARCHAR | 200 | NULL | 電話 |
| 所屬角色 | user_role | JSONB | - | NOT NULL | [user_role.id] |
| 最後登入時間 | last_login_at | TIMESTAMP | - | NULL | 最後登入 |
| 最後登入IP | last_login_ip | VARCHAR | 100 | NULL | IP位址 |
| 啟用 | is_active | BOOLEAN | 1 | DEFAULT=1 | 啟用狀態 |
| 編輯者 | edit_by | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 建立時間 | created_at | TIMESTAMP | - | NOT NULL | 建立時間 |
| 修改時間 | updated_at | TIMESTAMP | - | NULL | 修改時間 |

---

## 6. 使用者角色明細檔 (user_role)

### 用途
角色群組定義，用於權限管理。

### 資料表結構

```sql
CREATE TABLE user_role (
    -- 主鍵
    id SERIAL PRIMARY KEY,

    -- 角色資訊
    role_cname VARCHAR(200) NOT NULL,
    role_ename VARCHAR(200) NOT NULL,
    description TEXT,

    -- 系統欄位
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    edit_by INTEGER NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,

    -- 索引
    INDEX idx_user_role_active (is_active),

    -- 外鍵
    FOREIGN KEY (edit_by) REFERENCES user_detail(id)
);

-- 註解
COMMENT ON TABLE user_role IS '使用者角色明細檔';
COMMENT ON COLUMN user_role.description IS '說明 (富文本格式)';
```

### 欄位說明

| 欄位名稱 | 英文欄位 | 型別 | 長度 | 限制 | 說明 |
|---------|---------|------|------|------|------|
| 資料編號 | id | INTEGER | 8 | PK, AUTO | 自動編號 |
| 中文名稱 | role_cname | VARCHAR | 200 | NOT NULL | 角色中文名稱 |
| 英文名稱 | role_ename | VARCHAR | 200 | NOT NULL | 角色英文名稱 |
| 說明 | description | TEXT | - | NULL | 富文本格式 |
| 啟用 | is_active | BOOLEAN | 1 | DEFAULT=1 | 啟用狀態 |
| 編輯者 | edit_by | INTEGER | 8 | NOT NULL | 外鍵: user_detail.id |
| 建立時間 | created_at | TIMESTAMP | - | NOT NULL | 建立時間 |
| 修改時間 | updated_at | TIMESTAMP | - | NULL | 修改時間 |

---

## 資料表關聯圖

```
organizations (1) ──┬──< (N) user_detail
                    │
                    └──< (1) sys_profile

user_detail (1) ────┬──< (N) userlogs
                    │
                    └──< (N) user_detail (self-reference, edit_by)

user_role (1) ──────< (N) user_detail.user_role (JSONB array)

sysfuction (1) ─────┬──< (N) userlogs
                    │
                    └──< (N) sysfuction (self-reference, upper_func_id)
```

---

## 特殊約束說明

### 1. sys_profile 唯一性
- 只允許一筆資料 (id=1)
- 使用 `CHECK (id = 1)` 約束

### 2. organizations.is_mana 唯一性
- 只允許一家組織為系統管理公司
- 需要在應用層或觸發器確保唯一性

### 3. sysfuction 功能類型約束
- `func_type = 1` (節點): `func_module_name` 必須為 NULL
- `func_type = 2` (功能): `func_module_name` 必須有值

### 4. user_detail 初始資料
- 第一筆使用者的 `edit_by` 需要特殊處理（可設為自己的 id=1）

---

## 更新記錄

| 日期 | 版本 | 說明 |
|------|------|------|
| 2026-01-18 | 1.0.0 | 建立系統管理後台資料表設計文檔，包含 6 張資料表定義與預設資料 |
