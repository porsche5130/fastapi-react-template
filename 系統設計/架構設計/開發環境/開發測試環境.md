# 開發測試環境配置

## 專案資訊
- **專案名稱**: Paris Agreement Article 6.4 管理系統
- **專案代碼**: PA6.4
- **文件版本**: 1.4.0
- **最後更新**: 2026-01-18

---

## 一、環境架構概覽

```
本機開發環境
├── PostgreSQL (本機安裝)
│   ├── Host: localhost
│   ├── Port: 5432
│   └── Database: pa64_dev
│
├── Redis (本機安裝)
│   ├── Host: localhost
│   ├── Port: 6379
│   ├── 認證: 密碼驗證
│   └── Password: !DC1qaz2wsx
│
└── 應用服務 (W:\P-PA6.4\Develop)
    ├── Frontend (React + Vite)
    │   ├── 位置: W:\P-PA6.4\Develop\frontend
    │   ├── Port: 10180
    │   └── API Proxy: /api → http://localhost:10181
    │
    └── Backend (FastAPI)
        ├── 位置: W:\P-PA6.4\Develop\backend
        ├── Port: 10181
        └── 可透過 http://localhost:10180/api 存取
```

### Port 配置說明

| 服務 | Port | 用途 | 存取方式 |
|------|------|------|----------|
| **Frontend** | 10180 | 前端開發伺服器 | http://localhost:10180 |
| **Backend** | 10181 | 後端 API 伺服器 | http://localhost:10181 |
| **API (透過前端)** | 10180/api | 前端代理後端 API | http://localhost:10180/api |
| **PostgreSQL** | 5432 | 資料庫 | localhost:5432 |
| **Redis** | 6379 | 快取 | localhost:6379 |

---

## 二、PostgreSQL 環境

### 2.1 基本資訊

| 項目 | 內容 |
|------|------|
| **安裝位置** | 本機安裝 |
| **版本** | PostgreSQL 16.10 |
| **主機** | localhost |
| **Port** | 5432 |
| **資料庫名稱** | pa64_dev |
| **使用者** | dev |
| **密碼** | dev123 |
| **超級使用者** | postgres |
| **超級使用者密碼** | !DC1qaz2wsx |
| **最大連線數** | 200 |
| **目前連線數** | 8 |

### 2.2 連線字串

#### **本機連線**
```
postgresql://dev:dev123@localhost:5432/pa64_dev
```

#### **Docker 容器連線**
```
postgresql://dev:dev123@host.docker.internal:5432/pa64_dev
```

### 2.3 多專案共用

同一個 PostgreSQL 服務可管理多個專案的資料庫：

```sql
-- PA6.4 專案
CREATE DATABASE pa64_dev;

-- 其他專案
CREATE DATABASE ecommerce_dev;
CREATE DATABASE blog_dev;
```

### 2.4 測試驗證

#### **連線測試**
```bash
# 使用 dev 使用者測試
psql -h localhost -U dev -d pa64_dev -c "SELECT version();"

# 使用 postgres 超級使用者測試
psql -h localhost -U postgres -c "SELECT version();"
```

#### **功能測試**
```bash
# 執行完整測試腳本
cd W:\P-PA6.4
python testarea\test-database.py
```

#### **測試結果（2026-01-18）**
```
✓ 連線測試 - 成功
✓ 版本檢查 - PostgreSQL 16.10
✓ CREATE TABLE - 成功
✓ INSERT 操作 - 成功
✓ SELECT 操作 - 成功
✓ JSONB 查詢 - 成功
✓ UPDATE 操作 - 成功
✓ DELETE 操作 - 成功
✓ 連線資訊 - 8/200 連線
✓ 資料庫大小 - 7684 kB
```

---

## 三、Redis 環境

### 3.1 基本資訊

| 項目 | 內容 |
|------|------|
| **安裝位置** | 本機安裝 |
| **主機** | localhost |
| **Port** | 6379 |
| **版本** | 7.4.6 |
| **認證方式** | 密碼驗證（password-only） |
| **密碼** | !DC1qaz2wsx |
| **資料庫數量** | 16 (db0 - db15) |

### 3.2 連線字串

#### **本機連線**
```
redis://:!DC1qaz2wsx@localhost:6379/0
```

#### **Docker 容器連線**
```
redis://:!DC1qaz2wsx@host.docker.internal:6379/0
```

### 3.3 資料庫分配

使用不同的資料庫編號區分專案：

| 資料庫 | 專案 | 連線字串 |
|--------|------|---------|
| **db0** | PA6.4 | `redis://:!DC1qaz2wsx@host.docker.internal:6379/0` |
| **db1** | 其他專案 1 | `redis://:!DC1qaz2wsx@host.docker.internal:6379/1` |
| **db2** | 其他專案 2 | `redis://:!DC1qaz2wsx@host.docker.internal:6379/2` |
| **db3-15** | 保留 | - |

### 3.4 測試驗證

#### **連線測試**
```python
import redis

r = redis.Redis(
    host='localhost',
    port=6379,
    password='!DC1qaz2wsx',
    db=0,
    decode_responses=True
)

# PING 測試
print(r.ping())  # True

# 寫入測試
r.set('test', 'Hello Redis')
print(r.get('test'))  # Hello Redis
```

#### **基本操作**
```bash
# PING 測試
redis-cli -a "!DC1qaz2wsx" ping
# 返回: PONG

# 查看所有 key
redis-cli -a "!DC1qaz2wsx" -n 0 KEYS "*"

# 切換資料庫
redis-cli -a "!DC1qaz2wsx"
127.0.0.1:6379> SELECT 0  # PA6.4
127.0.0.1:6379> SELECT 1  # 其他專案
```

#### **測試結果（2026-01-18）**
```
✓ PING 測試 - True
✓ 寫入測試 - 成功
✓ 讀取測試 - 成功
✓ TTL 設定 - 成功（300秒）
✓ 多資料庫切換 - 成功（db0, db1, db2 獨立運作）
✓ 清理測試 - 成功
```

**伺服器資訊：**
- Redis 版本：7.4.6
- 作業系統：支援 Windows
- 可用資料庫：16 個（db0-db15）
- 認證方式：密碼驗證（password-only）

---

## 四、前後端服務配置

### 4.1 Frontend 開發伺服器 (Vite)

**Port**: 10180

**vite.config.ts**:

**位置**: `W:\P-PA6.4\Develop\frontend\vite.config.ts`

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 10180,
    host: '0.0.0.0',
    proxy: {
      '/api': {
        target: 'http://localhost:10181',
        changeOrigin: true
      },
      '/images': {
        target: 'http://localhost:10181',
        changeOrigin: true
      },
      '/uploads': {
        target: 'http://localhost:10181',
        changeOrigin: true
      },
      '/locales': {
        target: 'http://localhost:10181',
        changeOrigin: true
      }
    }
  }
});
```

**存取方式**:
- 前端應用: http://localhost:10180
- API 呼叫: http://localhost:10180/api (自動代理到後端 10181)

### 4.2 Backend 開發伺服器 (FastAPI + Uvicorn)

**Port**: 10181

**啟動指令**:
```bash
cd W:\P-PA6.4\Develop\backend
uvicorn app.main:app --host 0.0.0.0 --port 10181 --reload
```

**backend/.env**:

**位置**: `W:\P-PA6.4\Develop\backend\.env`

```bash
# 應用程式
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=DEBUG

# 服務 Port
PORT=10181

# 資料庫 (本機 PostgreSQL)
DATABASE_URL=postgresql://dev:dev123@localhost:5432/pa64_dev

# Redis (本機 Redis, db0)
REDIS_URL=redis://:!DC1qaz2wsx@localhost:6379/0

# 安全性
SECRET_KEY=dev-secret-key-change-in-production

# CORS
ALLOWED_ORIGINS=["http://localhost:10180"]
```

**存取方式**:
- 直接存取: http://localhost:10181
- 透過前端代理: http://localhost:10180/api

### 4.3 API 路由設計

| 前端呼叫 | 代理後實際路徑 | 用途 |
|----------|----------------|------|
| `http://localhost:10180/api/users` | `http://localhost:10181/api/users` | API 端點 |
| `http://localhost:10180/images/logo.svg` | `http://localhost:10181/images/logo.svg` | 圖片資源 |
| `http://localhost:10180/uploads/{file}` | `http://localhost:10181/uploads/{file}` | 上傳檔案 |
| `http://localhost:10180/locales/zh-TW/common.json` | `http://localhost:10181/locales/zh-TW/common.json` | 語系檔案 |

---

## 五、環境啟動流程

### 5.1 前置檢查

```bash
# 1. 檢查 PostgreSQL (本機安裝)
psql -h localhost -U dev -d pa64_dev -c "SELECT version();"

# 2. 檢查 Redis (本機安裝)
redis-cli -a "!DC1qaz2wsx" ping

# 3. 檢查前端依賴
cd W:\P-PA6.4\Develop\frontend
npm install

# 4. 檢查後端依賴
cd W:\P-PA6.4\Develop\backend
pip install -r requirements.txt
```

### 5.2 啟動服務

**終端機 1 - 啟動後端**:
```bash
cd W:\P-PA6.4\Develop\backend
uvicorn app.main:app --host 0.0.0.0 --port 10181 --reload
```

**終端機 2 - 啟動前端**:
```bash
cd W:\P-PA6.4\Develop\frontend
npm run dev
```

### 5.3 驗證服務

| 服務 | URL | 說明 |
|------|-----|------|
| **前端** | http://localhost:10180 | React 開發伺服器 (Vite) |
| **後端** | http://localhost:10181 | FastAPI 服務 |
| **API 文件** | http://localhost:10181/docs | Swagger UI |
| **前端 API 代理** | http://localhost:10180/api | 透過前端代理的後端 API |

---

## 六、優點說明

### 6.1 本機開發環境的優勢

1. **即時熱重載**
   - Vite 提供極快的 HMR (Hot Module Replacement)
   - Uvicorn `--reload` 偵測 Python 檔案變更自動重啟
   - 修改程式碼後立即生效

2. **除錯方便**
   - 可直接使用 IDE 的偵錯工具 (VSCode, PyCharm)
   - 設定中斷點 (Breakpoint)
   - 即時查看變數狀態

3. **資源節省**
   - 不需要為開發環境建置 Docker Image
   - PostgreSQL 和 Redis 持續運行在本機
   - 多專案可共用資料庫服務

4. **多專案共用**
   - 一個 PostgreSQL 管理多個資料庫
   - 一個 Redis 使用不同 db 編號 (db0, db1, db2...)

5. **資料持久化**
   - 資料存在本機，不會因專案目錄刪除而遺失
   - 方便備份和恢復

6. **快速切換**
   - 不同專案間快速切換 Port
   - 不需要重建 Docker 容器

---

## 七、注意事項

### 7.1 Port 衝突

確保 Port 10180 和 10181 沒有被其他程式佔用：

```bash
# Windows 檢查 Port 佔用
netstat -ano | findstr :10180
netstat -ano | findstr :10181

# 如果被佔用，終止程式
taskkill /PID <PID> /F
```

### 7.2 資料庫連線

開發環境使用 `localhost` 連線本機資料庫：

```bash
# PostgreSQL
DATABASE_URL=postgresql://dev:dev123@localhost:5432/pa64_dev

# Redis
REDIS_URL=redis://:!DC1qaz2wsx@localhost:6379/0
```

**注意**: 測試環境（NAS Docker）需使用 `host.docker.internal` 取代 `localhost`

### 7.3 環境變數檔案

開發環境的 `.env` 檔案**絕對不可提交到 Git**：

```bash
# 確認 .gitignore 包含
.env
.env.local
.env.*.local
__pycache__/
node_modules/
```

### 7.4 依賴更新

當 `package.json` 或 `requirements.txt` 更新時：

```bash
# Frontend
cd W:\P-PA6.4\Develop\frontend
npm install

# Backend
cd W:\P-PA6.4\Develop\backend
pip install -r requirements.txt
```

### 7.5 資料庫連線數

- PostgreSQL 最大連線數: 200 (已調整)
- Redis 預設最大連線數: 10000
- 開發環境目前連線數: 8/200
- 多專案共用時注意連線池設定

---

## 八、故障排除

### 8.1 PostgreSQL 連線失敗

```bash
# 檢查服務是否運行
psql -h localhost -U dev -d pa64_dev -c "SELECT 1;"

# 檢查 Docker 容器能否連線
docker run --rm postgres:15 psql -h host.docker.internal -U dev -d pa64_dev -c "SELECT 1;"
```

### 8.2 Redis 連線失敗

```bash
# 檢查服務是否運行
redis-cli -a "!DC1qaz2wsx" ping

# 檢查 Docker 容器能否連線
docker run --rm redis:7-alpine redis-cli -h host.docker.internal -p 6379 -a "!DC1qaz2wsx" ping
```

### 8.3 Docker 容器無法連接本機服務

可能原因：
1. 防火牆阻擋
2. `host.docker.internal` 不支援（Linux）
3. 服務未監聽 0.0.0.0

---

## 九、附錄

### 9.1 快速指令參考

```bash
# Frontend (Vite)
cd W:\P-PA6.4\Develop\frontend
npm run dev                                             # 啟動開發伺服器 (Port 10180)
npm run build                                           # 建置生產版本

# Backend (FastAPI)
cd W:\P-PA6.4\Develop\backend
uvicorn app.main:app --host 0.0.0.0 --port 10181 --reload  # 啟動開發伺服器
python -m pytest                                        # 執行測試

# PostgreSQL
psql -h localhost -U dev -d pa64_dev                    # 連線
pg_dump -h localhost -U dev pa64_dev > backup.sql       # 備份
psql -h localhost -U dev -d pa64_dev < backup.sql       # 還原

# Redis
redis-cli -a "!DC1qaz2wsx" -n 0                         # 連線 db0
redis-cli -a "!DC1qaz2wsx" --scan --pattern "pa64:*"    # 查找 key
redis-cli -a "!DC1qaz2wsx" -n 0 FLUSHDB                 # 清空 db0

# Docker
docker-compose -f docker-compose.dev.yml up -d          # 啟動
docker-compose -f docker-compose.dev.yml down           # 停止
docker-compose -f docker-compose.dev.yml logs -f        # 日誌
```

### 9.2 測試腳本位置

測試和驗證腳本位置：
```
W:\P-PA6.4\testarea\
├── test-database.py    # PostgreSQL 完整功能測試
└── check-postgres.py   # PostgreSQL 連線檢查工具
```

**執行測試：**
```bash
# PostgreSQL 測試
cd W:\P-PA6.4
python testarea\test-database.py

# PostgreSQL 連線檢查
python testarea\check-postgres.py
```

---

## 十、更新記錄

| 日期 | 版本 | 說明 |
|------|------|------|
| 2026-01-18 | 1.0.0 | 建立初始文檔，記錄 PostgreSQL 和 Redis 環境配置 |
| 2026-01-18 | 1.1.0 | 完成 PostgreSQL 和 Redis 實際測試，更新版本資訊和測試結果 |
| 2026-01-18 | 1.2.0 | 新增前後端 Port 配置（前端 10180、後端 10181），設定 API 代理 |
| 2026-01-18 | 1.3.0 | 更新開發環境目錄為 W:\P-PA6.4\Develop，統一所有路徑參照 |
| 2026-01-18 | 1.4.0 | 修正 5.2、5.3 為本機直接執行（非 Docker），更新 Port 為 10180/10181，調整注意事項 |
